<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <EditText
        android:id="@+id/activity_authorization_edit_text_login"
        android:layout_width="wrap_content"
        android:layout_height="48dp"
        android:width="70dp"
        android:textAlignment="center"
        android:layout_gravity="center"
        android:hint="login">
    </EditText>

    <EditText
        android:id="@+id/activity_authorization_edit_text_password"
        android:layout_width="wrap_content"
        android:layout_height="48dp"
        android:inputType="textPassword"
        android:hint="password"
        android:layout_gravity="center">
    </EditText>
    <Button
        android:id="@+id/activity_authorization_button_send"
        android:layout_width="wrap_content"
        android:layout_height="48dp"
        android:text="@string/activity_authorization_button_send"
        android:layout_gravity="center">
    </Button>

</LinearLayout>